.hero-section {
  min-height: 100vh;
  background: url("assets/images/psychedelic-image.jpg") center/cover no-repeat;
  position: relative;
  display: flex; 
  align-items: center; 
  justify-content: center;
  text-align: center;
}

/* Optional overlay */
.hero-section::before {
  content: "";
  position: absolute;
  inset: 0;
  background-color: rgba(0,0,0,0.3);
  z-index: 0;
}

.hero-container {
  position: relative;
  z-index: 1;
  color: #fff; 
  /* etc. */
}